<!DOCTYPE html>
<html>
  <head>
    <title>サインアップ - BecomeCreater</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <div class="container mt-5">
      <h2 class="text-center">新規登録</h2>

      <% if @user.errors.any? %>
          <ul class="alert alert-danger text-left" role="alert">
            <h6 class="alert-heading">
              <%= @user.errors.count %>件のエラーが発生しました
            </h6>
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        <% end %>



      <%= form_with(model: resource, as: resource_name, url: registration_path(resource_name), local: false, class: 'mt-4') do |f| %>
        <div class="mb-3">
          <%= f.label :name, class: 'form-label' %>
          <%= f.text_field :name, class: 'form-control', placeholder: 'ユーザー名' %>
          <small class="text-muted">3文字以上で入力してください。</small>
        </div>

        <div class="mb-3">
          <%= f.label :email, class: 'form-label' %>
          <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'メールアドレス' %>
        </div>

        <div class="mb-3">
          <%= f.label :password, class: 'form-label' %>
          <%= f.password_field :password, class: 'form-control', placeholder: 'パスワード' %>
          <small class="text-muted">6文字以上で設定してください。</small>
        </div>

        <div class="mb-3">
          <%= f.label :password_confirmation, class: 'form-label' %>
          <%= f.password_field :password_confirmation, class: 'form-control', placeholder: 'パスワード（確認用）' %>
        </div>

        <div class="d-flex justify-content-between align-items-center">
          <%= f.submit 'サインアップ', class: 'btn btn-primary' %>
        </div>
      <% end %>

      <div class="text-center mt-4">
        <p>すでにアカウントをお持ちの方は <%= link_to 'ログイン', new_user_session_path %></p>
      </div>
    </div>
  </body>
</html>
