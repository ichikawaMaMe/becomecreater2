<!DOCTYPE html>
<html>
  <head>
    <title>トップページ - BecomeCreater</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
    <div class="container mt-5">
      <div class="text-center mb-4">
        <h1 class="display-4">BecomeCreaterへようこそ</h1>
        <p class="lead">作品を共有し、仲間と成長しましょう。</p>
        <%= link_to 'ログイン', new_user_session_path, class: 'btn btn-primary btn-lg' %>
        <%= link_to '新規登録', new_user_registration_path, class: 'btn btn-secondary btn-lg ms-2' %>
      </div>

      <div class="row">
        <div class="col-md-6">
          <h3>人気の投稿</h3>
          <div class="list-group">
          <% if @top_ranked_posts.present? %>
          <% @top_ranked_posts.each do |post| %>
          <a href="<%= post_path(post) %>" class="list-group-item list-group-item-action">
           <img src="<%= post.image.attached? ? url_for(post.image) : asset_path('sample-author1.jpg') %>" alt="<%= post.title %>" class="img-fluid">
           <h5 class="mt-2"><%= post.title %></h5>
          </a>
          <% end %>
          <% else %>
          <p>トップの投稿はありません。</p>
          <% end %>
         </div>
      </div>
    </div>
  </body>
</html>
